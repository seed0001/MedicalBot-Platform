{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build --workspace @medbot/shared && npm run build --workspace @medbot/conditions && npm run build --workspace @medbot/api"
  },
  "deploy": {
    "startCommand": "npm run db:migrate --workspace @medbot/api && npm run start --workspace @medbot/api",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
